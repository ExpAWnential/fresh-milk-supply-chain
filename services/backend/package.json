{
  "name": "@fresh-milk/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @fresh-milk/storage build && pnpm build && node --test test/*.test.mjs",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@fresh-milk/storage": "workspace:*",
    "@grpc/grpc-js": "^1.14.0",
    "@hyperledger/fabric-gateway": "^1.6.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.15.0",
    "typescript": "^5.5.0"
  }
}
