# Fabric Network

This package will hold TypeScript wrappers for local Hyperledger Fabric network lifecycle commands.

The wrappers are placeholders for now. A later step should connect them to the selected Fabric topology, Docker Compose files and Fabric CLI commands.
