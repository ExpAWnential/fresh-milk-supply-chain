# Certificates for the processor organisation. One peer, one admin and one user: the user is the
# identity the processor signs its business transactions with.
PeerOrgs:
  - Name: Processor
    Domain: processor.example.com
    EnableNodeOUs: true
    Template:
      Count: 1
      SANS:
        - localhost
    Users:
      Count: 1
