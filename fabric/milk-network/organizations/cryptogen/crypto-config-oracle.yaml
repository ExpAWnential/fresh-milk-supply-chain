# Certificates for the oracle organisation. One peer, one admin and one user: the user is the
# identity the oracle signs its business transactions with.
PeerOrgs:
  - Name: Oracle
    Domain: oracle.example.com
    EnableNodeOUs: true
    Template:
      Count: 1
      SANS:
        - localhost
    Users:
      Count: 1
