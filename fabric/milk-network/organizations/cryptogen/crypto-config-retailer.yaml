# Certificates for the retailer organisation. One peer, one admin and one user: the user is the
# identity the retailer signs its business transactions with.
PeerOrgs:
  - Name: Retailer
    Domain: retailer.example.com
    EnableNodeOUs: true
    Template:
      Count: 1
      SANS:
        - localhost
    Users:
      Count: 1
