# Certificates for the regulator organisation. One peer, one admin and one user: the user is the
# identity the regulator signs its business transactions with.
PeerOrgs:
  - Name: Regulator
    Domain: regulator.example.com
    EnableNodeOUs: true
    Template:
      Count: 1
      SANS:
        - localhost
    Users:
      Count: 1
